---
- hosts: all
  tasks:
    - name: running VMs
      ansible.builtin.shell:
        cmd: cat runningvms.lst
        chdir: /Users/ansible
      register: vbvm
    - debug: 
         msg: "{{vbvm.stdout}}"
