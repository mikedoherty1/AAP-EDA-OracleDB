---
- hosts: all
  tasks:
    - name: running VMs
      ansible.builtin.shell:
        cmd: cat runningvms.lst
        chdir: /Users/ansible
      register: vbvm
    - name: running VMs
      ansible.builtin.shell:
        cmd: /usr/local/bin/VBoxManage showvminfo "{{vbvm.stdout}}" --machinereadable |grep macaddress 
      register: shell
    - debug: var=shell.stdout_lines
