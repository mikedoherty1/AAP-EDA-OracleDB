---
  - name: Add New VBox server to control.local /etc/hosts file
    vars:
    hosts: all
    tasks:    
      lineinfile:
        path:  /etc/hosts
        line: "{{ vbvmip }}     {{ vbname }}"





